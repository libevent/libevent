name: FreeBSD
on:
  push:
    branches:
      - '*'
    paths:
      - '**.c'
      - '**.h'
      - '**.am'
      - '**.m4'
      - 'cmake/*'
      - 'extra/*'
      - '.github/workflows/FreeBSD.yml'
  pull_request:
    branches:
      - '*'
    paths:
      - '**.c'
      - '**.h'
      - '**.am'
      - '**.m4'
      - 'cmake/*'
      - 'extra/*'
      - '.github/workflows/FreeBSD.yml'



jobs:
  FreeBSD:
    runs-on: macos-12
    steps:
    - uses: actions/checkout@v2
    - uses: vmactions/freebsd-vm@v0
      with:
        prepare: |
          pkg install -y  mbedtls cmake
        usesh: true
        run: |
          mkdir build
          cd build
          cmake ..
          make && make verify




